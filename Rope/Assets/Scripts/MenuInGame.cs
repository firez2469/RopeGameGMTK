using UnityEngine;
using UnityEngine.UI;

public class MenuInGame : MonoBehaviour
{
    // This script is when you need to change some setting or the player needs to quit the game
    // It can do it my just pressing "ESC" and opening the in game Menu.

    // Update is called once per frame
    void Update()
    {
        if (Input.GetKey(KeyCode.Escape))
        {

        }
    }
}
